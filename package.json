{
  "name": "nft-events-server-docker-compose",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "docker-compose up -d --build --scale scanner=1",
    "stop": "docker-compose down",
    "tail": "docker-compose logs -f",
    "dev": "npm run start && npm run tail"
  },
  "author": "Dan Haber",
  "license": "WAT"
}
